<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Fukuoka view</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <%= stylesheet_link_tag 'user.show', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

    <main>
          <div class="my-1">
            <div class="container mt-5">
                <div class="row">
                    <div class="col-10 mx-auto">
                      <div class="mypage">
                        <p>マイページ</p>
                      </div>
                        <div class="card">
                          <div class="user-avatar" id="avatar"> <!-- ① -->
                            <% if @user.avatar.attached? %>
                              <%= image_tag url_for(@user.avatar), class: "mb-2 current-avatar", id: "avatar_preview" %>
                            <% else %>
                              <label for="avatar"> <!-- 修正：idをavatarに変更 -->
                                <%= image_tag "default_avatar.jpg", class: "mb-2 current-avatar", id: "avatar_preview" %>
                                <input type="file" name="avatar" id="avatar" style="display: none;"> <!-- 修正：input要素を追加 -->
                              </label>
                            <% end %>
                          </div>              
                                <div class="card-body">
                                <h5 class="card-title"><%= current_user.name %></h5>
                                <h6 class="cars-subtitle mb-2 text=muted"><%= current_user.email %></h6>
                                <div class="right">
                                    <%= link_to '編集', edit_user_path %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="mypage">
          <p>あなたの投稿一覧</p>
        </div>
        <% if @blogs.any? %>
          <% @blogs.each do |blog| %>
          <%= render blog %> 
        <% end %>
        <% else %>
          <div class="mypagee">
            <%= link_to "投稿してみる", new_blog_path %>
          </div>
        <% end %>
    </main>
  
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        const fileInput = document.querySelector('#avatar'); // 修正：idをavatarに変更
        const imagePreview = document.querySelector('#avatar_preview');
    
        fileInput.addEventListener('change', function() {
          const file = this.files[0];
    
          if (file) {
            const reader = new FileReader();
    
            reader.addEventListener('load', function() {
              imagePreview.src = reader.result;
            });
    
            reader.readAsDataURL(file);
          } else {
            // No file selected, show the default icon
            imagePreview.src = ''; // Clear the preview
          }
        });
      });
    </script>
    

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
</html>
